local Dialogues = {}

local dialogues = {
	outside = {
		car = {"A vintage dsadsa"},
	}
}

function Dialogues.get(main, sub)
	@@assert(type(main) == "string")
	@@assert(type(sub) == "string")
	@@assert(dialogues[id])
	@@assert(dialogues[id][sub])
	return dialogues[id][sub]
end

return Dialogues
