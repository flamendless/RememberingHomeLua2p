local Concord = require("modules.concord.concord")

Concord.component("hover_change_color", function(c, color, duration)
	!if _ASSERT then
	Assert.color(color)
	Assert.must(duration, Types.T_NUMBER)
	!end

	c.color = {unpack(color)}
	c.duration = duration
end)
