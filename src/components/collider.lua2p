local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("collider", function(c, size)
	!if _ASSERT then
	Assert.should_vec2(size)
	!end

	c.size = size:copy()
end)
