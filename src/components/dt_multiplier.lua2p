local Concord = require("modules.concord.concord")

Concord.component("dt_multiplier", function(c, mul)
	!if _ASSERT then
	assert(type(mul) == "number", "Passed argument must be of type 'number'")
	!end
	c.mul = mul
end)
