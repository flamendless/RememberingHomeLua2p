local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("dt_multiplier", function(c, mul)
	!if _ASSERT then
	Assert.should_number(mul)
	!end

	c.mul = mul
end)
