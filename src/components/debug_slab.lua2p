local Concord = require("modules.concord.concord")

Concord.component("debug_slab", function(c, id)
	!if _ASSERT then
	assert(type(id) == "string", "Passed argument must be of type 'string'")
	!end
	c.id = id
end)
