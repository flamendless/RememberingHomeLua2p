local Concord = require("modules.concord.concord")

Concord.component("paint")

Concord.component("text_with_paint", function(c, id)
	!if _ASSERT then
	Assert.must(id, Types.T_STRING)
	!end

	c.id = id
	c.paint = nil
end)
