local Concord = require("modules.concord.concord")

Concord.component("random_text", function(c, text)
	@@assert(type(text) == "string" or type(text) == "table")
	if type(text) == "string" then
		c.use_string = true
	elseif type(text) == "table" then
		c.use_table = true
	end
	c.value = text
end)
