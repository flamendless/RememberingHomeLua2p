local Concord = require("modules.concord.concord")

Concord.component("hold", function(c, max_time)
	!if _ASSERT then
	Assert.must(max_time, Types.T_NUMBER)
	!end

	c.current_time = 0
	c.max_time = max_time
end)
