local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("z_index", function(c, z)
	!if _ASSERT then
	Assert.should_number(z)
	!end

	c.z = z
end)
