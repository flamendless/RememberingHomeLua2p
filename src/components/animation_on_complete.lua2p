local Concord = require("modules.concord.concord")

Concord.component("animation_on_complete", function(c, callback, event)
	!if _ASSERT then
	Assert.should_function(callback)
	Assert.safe_either_value(event, "pauseAtEnd", "pauseAtStart")
	!end

	c.callback = callback
	c.event = event
end)
