local Concord = require("modules.concord.concord")

Concord.component("text_paint_data", function(c, id, data)
	!if _ASSERT then
	Assert.must(id, Types.T_STRING)
	Assert.must(data, Types.T_TABLE)
	!end

	c.id = id
	c.data = data
end)
