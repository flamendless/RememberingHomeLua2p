local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("quad", function(c, quad)
	!if _ASSERT then
	Assert.should_quad(quad)
	!end

	c.quad = quad
end)
