local Concord = require("modules.concord.concord")

Concord.component("arc", function(c, radius, segments)
	!if _ASSERT then
	Assert.should_number(radius)
	Assert.should_number(segments)
	!end

	c.radius = radius
	c.segments = segments
end)
