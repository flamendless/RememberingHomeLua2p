local Concord = require("modules.concord.concord")

Concord.component("sprite", function(c, image, name)
	@@assert(image:type() == "Image")
	@@sassert(name, type(name) == "string")
	c.image = image
	c.name = name
end)
