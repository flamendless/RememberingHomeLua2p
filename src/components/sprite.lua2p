local Concord = require("modules.concord.concord")

Concord.component("sprite", function(c, sprite, name)
	!if _ASSERT then
	Assert.must(sprite, Types.T_IMAGE)
	Assert.safe.must(name, Types.T_STRING)
	!end

	c.sprite = sprite
	c.name = name
end)
