local Concord = require("modules.concord.concord")

Concord.component("glitch_colors", function(c, colors)
	!if _ASSERT then
	assert(type(colors) == "table", "Passed argument must be of type 'table'")
	!end
	c.current = 1
	c.colors = colors
end)
