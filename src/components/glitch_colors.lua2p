local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("glitch_colors", function(c, colors)
	!if _ASSERT then
	Assert.should_table(colors)
	!end

	c.current = 1
	c.colors = colors
end)
