local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("gravity", function(c, gravity)
	!if _ASSERT then
	Assert.should_number(gravity)
	!end

	c.gravity = gravity
end)
