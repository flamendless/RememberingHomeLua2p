local Concord = require("modules.concord.concord")

Concord.component("position", function(c, pos)
	!if _ASSERT then
	assert(pos.type == "vec2", "Passed argument must be of type 'vec2'")
	!end
	c.pos = pos:copy()
	c.original_pos = c.pos:copy()
end)
