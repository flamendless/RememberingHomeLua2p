local Concord = require("modules.concord.concord")

Concord.component("lerp_on_complete", function(c, callback)
	!if _ASSERT then
	Assert.must(callback, Types.T_FUNCTION)
	!end

	c.callback = callback
end)
