local Concord = require("modules.concord.concord")

Concord.component("bounding_box", function(c, pos, size)
	!if _ASSERT then
	Assert.is(pos, Types.T_VEC2)
	Assert.is(size, Types.T_VEC2)
	!end

	c.pos = pos
	c.size = size
	c.screen_size = pos:copy()
end)
