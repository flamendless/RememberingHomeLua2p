local Concord = require("modules.concord.concord")

Concord.component("text", function(c, text)
	!if _ASSERT then
	Assert.must(text, Types.T_STRING)
	!end

	c.text = text
end)

Concord.component("static_text", function(c, text)
	!if _ASSERT then
	Assert.must(text, Types.T_STRING)
	!end

	c.text = text
	c.text_obj = nil
end)
