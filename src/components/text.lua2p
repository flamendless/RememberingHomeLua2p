local Concord = require("modules.concord.concord")

!if _ASSERT then
local Assert = require("assert")
!end

Concord.component("text", function(c, text, font)
	!if _ASSERT then
	Assert.should_string(text)
	Assert.safe_should_font_or_sdf(font)
	!end

	c.text = text
	c.font = font
end)
