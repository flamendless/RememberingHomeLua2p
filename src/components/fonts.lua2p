local Concord = require("modules.concord.concord")

Concord.component("fonts", function(c, fonts)
	!if _ASSERT then
	assert(type(fonts) == "table", "Passed argument must be of type 'table'")
	!end
	c.fonts = fonts
end)
