local Concord = require("modules.concord.concord")

Concord.component($_C_OPTION_KEY, function(c, id, page)
	@@assert(type(id) == "number")
	@@assert(type(page) == "number")
	c.id = id
	c.page = page
end)

Concord.component($_C_OPTION_DISABLED)
