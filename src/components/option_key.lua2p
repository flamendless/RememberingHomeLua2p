local Concord = require("modules.concord.concord")

Concord.component("option_key", function(c, id, page)
	!if _ASSERT then
	Assert.must(id, Types.T_NUMBER)
	Assert.must(page, Types.T_NUMBER)
	!end

	c.id = id
	c.page = page
end)

Concord.component("option_disabled", function(c)
end)
