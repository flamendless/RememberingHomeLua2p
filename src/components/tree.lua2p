local Concord = require("modules.concord.concord")

Concord.component("bg_tree", function(c, is_cover)
	!if _ASSERT then
	Assert.safe.must(is_cover, Types.T_BOOLEAN)
	!end

	c.is_cover = is_cover
end)
