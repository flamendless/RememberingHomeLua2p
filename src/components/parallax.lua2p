local Concord = require("modules.concord.concord")

Concord.component("parallax", function(c, speed)
	!if _ASSERT then
	Assert.must(speed, Types.T_VEC2)
	!end

	c.speed = speed
end)

Concord.component("parallax_stop", function(c)
end)
