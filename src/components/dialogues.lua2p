local Concord = require("modules.concord.concord")

Concord.component("dialogues", function(c, id, dialogues)
	!if _ASSERT then
	Assert.must(id, Types.T_STRING)
	Assert.must(dialogues, Types.T_TABLE)
	!end

	c.id = id
	c.dialogues = dialogues
	c.current = 1
	c.max_n = #dialogues
end)
