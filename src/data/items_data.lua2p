local ItemsData = {}

local items_desc = {
	item_backdoor_key = "The key for the backdoor",
}

function ItemsData.get_desc(id)
	@@assert(type(id) == "string")
	@@assert(items_desc[id])
	return items_desc[id]
end

return ItemsData
