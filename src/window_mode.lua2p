local WindowMode = {}

WindowMode.current = 1
WindowMode.modes = !(_WINDOW_MODES)
WindowMode.list = !(_WINDOW_MODES_STR)

function WindowMode.getCurrent()
	return WindowMode.modes[WindowMode.current]
end

return WindowMode
