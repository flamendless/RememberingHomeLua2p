local Concord = require("modules.concord.concord")

local RenderRect = Concord.system({
	pool = {"rect", "pos", "color", "draw_mode", "!nf_renderer"},
})

function RenderRect:draw_rect()
	for _, e in ipairs(self.pool) do
		self:draw_rect_ex(e)
	end
end

return RenderRect
